on: push
name: Deploy on Push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: notsan.steiiin.de
        username: sync@notsan.steiiin.de
        password: ${{ secrets.notsan_ftp_password }}
